export enum ArrowIcon {
  UP = 'icon-arrow-up',
  DOWN = 'icon-arrow-down',
}

export enum FilterType {
  startsWith = 'startsWith',
  includes = 'includes'
}

export enum TipoProducto {
  AUTOS = 'autos',
  TECNOLOGIA_PROTEGIDA = 'tp'
}

export enum EstadosValidacionClave {
  Exito = 1,
  Error = 2,
  Cancelada = 3
}
